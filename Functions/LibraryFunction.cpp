#include <iostream>
#include <cmath>
using namespace std;

int main() {
    float a;
    cin>>a;
    cout<<sqrt(a)<<endl<<cbrt(a)<<endl<<pow(a,2)<<endl<<pow(a,3);// library functions 
    return 0;
}